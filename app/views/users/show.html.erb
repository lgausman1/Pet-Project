<% if current_user == @user %>
<div class="jumbotron">
<div class="container">
<h3>Hello, <%= @user.first_name.capitalize %></h3></div></div>
<% else %>
<div class="jumbotron">
<div class="container">
<h3><%= @user.first_name.capitalize %>'s page</h3></div></div>
<% end %>

<% @pets.each do |pet| %>

	<%= pet.name %> <br>
	<img src="<%= pet.thumbnail %>	">
	<%= pet.breed %> <br>
	<%= pet.species %> <br>
	<%= pet.age %> <br>
	<%= pet.gender %>
	<h3> ACTIVITY LEVEL</h3><%= pet.activity_level%>
<% end %>













<!-- logout button -->
<%= button_to "Logout", logout_path, class: "btn btn-primary", method: :delete %>